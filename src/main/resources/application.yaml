spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-name: google
            client-id:
            client-secret:
            redirect-uri: http://localhost:8080/login/oauth2/code/google
            authorization-grant-type: authorization_code
            scope: profile, email
  jwt:
    secret: fjdsioafjdsaiofjoiwjhqowijdsoidsdjfd

  datasource:
    url: jdbc:postgresql://auth_db:5430/auth_db
    username: pinting
    password: pinting_pw
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create

    show-sql: true
    properties:
      hibernate:
        format_sql: true